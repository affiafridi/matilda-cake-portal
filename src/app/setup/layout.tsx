import type { ReactNode } from "react";

export const metadata = {
  title: "Setup — Order Portal",
};

export default function SetupLayout({ children }: { children: ReactNode }) {
  return children;
}
