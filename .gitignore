# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit real secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# vercel
.vercel

# dev-only image uploads (TODO: production storage will move to GCS)
# Brand assets that live alongside (logo.png, favicon.webp, etc.) are kept;
# only the order-items subfolder is ignored.
/public/uploads/order-items/
/public/uploads/flow-images/
/public/uploads/media/

# Local dev tools
cloud-sql-proxy.exe
commands.txt

# typescript
*.tsbuildinfo
next-env.d.ts
